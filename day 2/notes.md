When I compile a Java program using javac, the .java file is converted into a .class file.

This .class file contains bytecode, which is a platform-independent format understood by the JVM.

When I run the program using java, the JVM reads the bytecode and executes it step by step.s